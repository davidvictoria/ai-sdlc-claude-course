{
  "name": "ai-sdlc-claude-course",
  "version": "1.0.0",
  "private": true,
  "description": "Repositorio de laboratorio para el curso de AI-SDLC: servicio de pagos ficticio para practicar exploracion de codigo con agentes de IA.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "verify": "npm run typecheck && npm run lint && npm run test"
  },
  "devDependencies": {
    "@eslint/js": "^9.9.1",
    "eslint": "^9.9.1",
    "typescript": "^5.5.4",
    "typescript-eslint": "^8.3.0",
    "vitest": "^2.0.5"
  }
}
